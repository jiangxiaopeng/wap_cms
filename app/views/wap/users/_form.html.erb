<%= form_for(@wap_user) do |f| %>
  <% if @wap_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wap_user.errors.count, "error") %> prohibited this wap_user from being saved:</h2>

      <ul>
      <% @wap_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
